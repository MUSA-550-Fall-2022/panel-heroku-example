build:
  docker:
    web: Dockerfile
run:
  config:
    APP_NAME: gentle-earth-18442
  web: /env/bin/panel serve --address="0.0.0.0" --port=$PORT app.ipynb --allow-websocket-origin=$APP_NAME.herokuapp.com